Write a C function of O(n) order for removing all odd numbers from a given array. Example, if the array contains 10, 2, 3, 7, 8, 6, and 11. The output should be 10, 2, 8, and 6.

#include <stdio.h>

int main(){
    int arr[] = {10, 2, 3, 7, 8, 6, 11};
    int n = sizeof(arr)/sizeof(arr[0]);
    int idx = 0;
    for(int i = 0; i < n; i++){
        if(arr[i] % 2 == 0){
            arr[idx] = arr[i];
            idx++;
        }
    }

    for(int i = 0; i<idx; i++){
        printf("%d", arr[i]);
    }

    return 0;
}

// Write a recursive function to display elements of stack using the operations push() and pop() without violating the LIFO concept.

